# Empty by default to use same-origin /api requests.
# Uncomment only if you explicitly want to bypass proxy.
# VITE_API_URL=http://localhost:5080

# Optional tenant fallback used when URL/query cannot infer tenant slug.
# VITE_TENANT_FALLBACK=clinic1
